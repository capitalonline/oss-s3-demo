package main

func main()  {
	//var bucket = "demo-test"
	//ret := Server.SelectBuckets()
	//ret := Server.CreateBucket("demo-test1")
	//ret := Server.DeleteBucket("demo-test1")
	//ret := Server.GetBucketAcl(bucket)
	//ret := Server.SetBucketAcl(bucket, "public-read")
	//ret := Server.ListObjectAllfiles(bucket)
	//ret := Server.UploadFile(bucket, "./demo-go.go", "dd3.bin")
	//ret := Server.ListObjectVersions(bucket, "dd")
	//ret := Server.LikeListObjectfiles(bucket, "dd")
	//ret := Server.ListObjectDirs(bucket)
	//ret := Server.ListObjectFiles(bucket)
	//ret := Server.ListPdirDirs(bucket, "Top/")
	//ret := Server.ListPdirFiles(bucket, "Top/images")
	//ret := Server.DeleteObjectVersion(bucket, "log/dd1.bin", "")
	//ret := Server.CopyObject(bucket, bucket, "log/dd.py", "log/cp.pdf")
	//ret := Server.AddObjectRet(bucket, "log/dd.py", nil)
	//ret := Server.DeleteMatchObjectsFocusOnVersionId(bucket, "mm")
	//ret := Server.DownloadVersion(bucket, "log/worker.pdf", "./ww.pdf", "")
	//ret := Server.UploadLargeFile(bucket,"./ww.pdf", "log/cp.pdf", 20*1024*1024, false)
	//ret := Server.GetObjectAcl(bucket, "log/worker.pdf", nil)
	//fmt.Println(ret)
}
